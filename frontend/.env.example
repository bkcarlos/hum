# Base URL for the Go backend API. Default works with the Vite dev proxy
# (vite.config.ts proxies /api -> http://localhost:8080). Override for prod.
VITE_API_BASE=/api

# Shown to Apple in the MusicKit authorization dialog.
VITE_APP_NAME=Hum
VITE_APP_BUILD=0.5.0
